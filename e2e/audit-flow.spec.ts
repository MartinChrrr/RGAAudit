import { test, expect } from '@playwright/test';

test.describe('Audit flow', () => {
  test('placeholder — audit flow e2e', async ({ page }) => {
    // TODO: implémenter dans les étapes suivantes
    expect(true).toBe(true);
  });
});

{
  "name": "@rgaaudit/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Moteur d'audit RGAA 4.1 — crawl, analyse axe-core, mapping, rapport",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "test:coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.9.1",
    "axe-core": "^4.9.1",
    "fast-xml-parser": "^5.4.1",
    "playwright": "^1.42.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "vitest": "^3.0.5"
  }
}
